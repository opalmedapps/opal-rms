<?php declare(strict_types = 1);

class PDOStatement
{
    /**
     * @return list<array<string,string>>
     */
    //technically, the return type should be array<int,array<string,string|null>> but we're assuming that ORMS only uses INNER JOINs in queries...
    function fetchAll(): array {}
}

?>
